SECRET_KEY=change-this-to-a-random-secret-in-production
DB_PATH=./poiro.db

# Optional: Set your Anthropic API key for real AI generation
# If not set, the app uses a built-in mock AI provider
ANTHROPIC_API_KEY=

# Force mock AI even if API key is set (useful for testing)
USE_MOCK_AI=false
